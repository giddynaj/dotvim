Installation

	git clone git://github.com/giddynaj/dotvim.git ~/.vim

Create symlinks:

  	ln -s ~/.vim/vimrc ~/.vimrc
	ln -s ~/.vim/gvimrc ~/.gvimrc

Working with Submodules (top level of repo):
  (new clone only) git submodule init
  (update) git submodule update
